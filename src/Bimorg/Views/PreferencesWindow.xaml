<Window x:Class="Bimorg.Views.PreferencesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Preferences"
        Height="Auto"
        Width="420"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        TextElement.Foreground="{DynamicResource MahApps.Brushes.ThemeForeground}">
  <Window.Resources>
    <Style TargetType="Label">
      <Setter Property="Margin" Value="0,0,0,6" />
    </Style>
  </Window.Resources>

  <Grid Margin="18">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Label Grid.Row="0" FontWeight="SemiBold" Content="Theme" />

    <ComboBox Grid.Row="1"
              Margin="0,0,0,20"
              MinHeight="36"
              Padding="8,6"
              DisplayMemberPath="Label"
              SelectedValuePath="Value"
              ItemsSource="{Binding ThemeOptions}"
              SelectedValue="{Binding SelectedTheme, Mode=TwoWay}" />

    <StackPanel Grid.Row="2"
                Orientation="Horizontal"
                HorizontalAlignment="Right">
      <Button MinWidth="88"
              MinHeight="32"
              Margin="0,0,10,0"
              Padding="12,6"
              IsDefault="True"
              Content="OK"
              Click="Ok_OnClick" />
      <Button MinWidth="88"
              MinHeight="32"
              Padding="12,6"
              IsCancel="True"
              Content="Cancel" />
    </StackPanel>
  </Grid>
</Window>
